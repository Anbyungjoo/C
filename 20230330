- 3월 30일

```C
#include<stdio.h>

int main(void)
{
    char c;
    int i;
    float f;

    c = 10000; //내림 변환
    i = 1.23456 + 10  // 내림 변환
    f = 10+20; //올림 변환
    printf("c=%d, i=%d, f=%f\n",c,i,f);
    return 0;
}
```


```c
#include<stdio.h>

int main(void)
{
    int number;
    printf("정수를 입력하시오 :");
    scanf("%d",&number);

    if(number > 0)
    {
        printf("양수입니다\n");
    }
    printf("입력한 값은 %d입니다.", number);

    return 0;
}
```
